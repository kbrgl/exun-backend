#!/usr/bin/env node

const server = require('../server');
const config = require('../config');

const { port } = config;

server.listen(port, () => {
  console.log(`🎉 Listening on port ${port}`);
});
